name: Test the app


on: pull_request:
  branches: [ master ]


jobs:
  test_pull_request:
    runs_on: ubuntu-latest
    steps:
      - use: actions/checkout@v2
      - use: actions/setup-node@v1
        with:
          node-version: 12
      - run: npm ci
      - run: npm run test